# Performance

This document provides guidelines for writing performant code and identifies potential performance bottlenecks.

* **Avoid unnecessary object allocations.**
* **Use `remember` to cache expensive calculations in Compose.**
* **Use `lazy` lists for long lists.**
* **Use `constraintlayout` to optimize complex layouts.**
* **Profile your app to identify performance bottlenecks.**
